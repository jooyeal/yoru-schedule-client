import React from "react";
import styled from "styled-components";

const ComponentContainer = styled.div`
  position: absolute;
  bottom: 0;
`;

export default function Footer() {
  return <ComponentContainer>se</ComponentContainer>;
}
